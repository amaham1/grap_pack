<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.cms.admin.content.mapper.AdminContentTypeMapper">

    <!-- 콘텐츠 종류 결과 매핑 -->
    <resultMap id="contentTypeResultMap" type="AdminContentType">
        <id property="contentTypeId" column="content_type_id"/>
        <result property="typeName" column="type_name"/>
        <result property="typeDesc" column="type_desc"/>
        <result property="isActive" column="is_active"/>
        <result property="createDt" column="create_dt"/>
        <result property="updateDt" column="update_dt"/>
    </resultMap>

    <!-- 활성화된 콘텐츠 종류 목록 조회 -->
    <select id="selectActiveContentTypeList" resultMap="contentTypeResultMap">
        SELECT content_type_id, type_name, type_desc, is_active, create_dt, update_dt
        FROM content_type
        WHERE is_active = TRUE
        ORDER BY create_dt DESC
    </select>

    <!-- 전체 콘텐츠 종류 목록 조회 -->
    <select id="selectAllContentTypeList" resultMap="contentTypeResultMap">
        SELECT content_type_id, type_name, type_desc, is_active, create_dt, update_dt
        FROM content_type
        ORDER BY create_dt DESC
    </select>

    <!-- 콘텐츠 종류 상세 조회 -->
    <select id="selectContentTypeById" parameterType="long" resultMap="contentTypeResultMap">
        SELECT content_type_id, type_name, type_desc, is_active, create_dt, update_dt
        FROM content_type
        WHERE content_type_id = #{contentTypeId}
    </select>

    <!-- 콘텐츠 종류 등록 -->
    <insert id="insertContentType" parameterType="AdminContentType"
            useGeneratedKeys="true" keyProperty="contentTypeId">
        INSERT INTO content_type (type_name, type_desc, is_active, create_dt, update_dt)
        VALUES (#{typeName}, #{typeDesc}, #{isActive}, NOW(), NOW())
    </insert>

    <!-- 콘텐츠 종류 수정 -->
    <update id="updateContentType" parameterType="AdminContentType">
        UPDATE content_type
        SET type_name = #{typeName},
            type_desc = #{typeDesc},
            is_active = #{isActive},
            update_dt = NOW()
        WHERE content_type_id = #{contentTypeId}
    </update>

    <!-- 콘텐츠 종류 삭제 -->
    <delete id="deleteContentType" parameterType="long">
        DELETE FROM content_type
        WHERE content_type_id = #{contentTypeId}
    </delete>

</mapper>
