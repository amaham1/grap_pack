<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.grap.pack.admin.dashboard.mapper.AdminDashboardMapper">

    <select id="countPublishedContents" resultType="long">
        SELECT COUNT(*)
        FROM content
        WHERE delete_dt IS NULL
          AND is_published = TRUE
    </select>

    <select id="countUnpublishedContents" resultType="long">
        SELECT COUNT(*)
        FROM content
        WHERE delete_dt IS NULL
          AND is_published = FALSE
    </select>

    <select id="countPendingExternalData" resultType="long">
        SELECT
            COALESCE((SELECT COUNT(*) FROM festivals WHERE confirm_status = 'PENDING' OR confirm_status IS NULL), 0)
          + COALESCE((SELECT COUNT(*) FROM exhibitions WHERE confirm_status = 'PENDING' OR confirm_status IS NULL), 0)
          + COALESCE((SELECT COUNT(*) FROM welfare_services WHERE confirm_status = 'PENDING' OR confirm_status IS NULL), 0)
    </select>

</mapper>
