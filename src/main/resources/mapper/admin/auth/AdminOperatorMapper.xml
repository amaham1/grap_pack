<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.grap.pack.admin.auth.mapper.AdminOperatorMapper">

    <resultMap id="adminOperatorResultMap" type="co.grap.pack.admin.auth.model.AdminOperator">
        <id property="id" column="id"/>
        <result property="loginId" column="login_id"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="role" column="role" javaType="co.grap.pack.admin.auth.model.AdminOperatorRole"/>
        <result property="isActive" column="is_active"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByLoginId" resultMap="adminOperatorResultMap">
        SELECT *
        FROM admin_operator
        WHERE login_id = #{loginId}
    </select>

    <update id="updateLastLoginAt">
        UPDATE admin_operator
        SET last_login_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
