<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.grap.pack.qrmanage.superadmin.shopadmin.mapper.QrManageSuperShopAdminMapper">

    <!-- 상점관리자 ResultMap -->
    <resultMap id="shopAdminResultMap" type="co.grap.pack.qrmanage.shopadmin.auth.model.QrManageShopAdmin">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 상점관리자 목록 조회 (페이징) -->
    <select id="findAll" resultMap="shopAdminResultMap">
        SELECT id, username, password, name, email, phone, status, created_at, updated_at
        FROM qr_manage_shop_admin
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    name LIKE CONCAT('%', #{keyword}, '%')
                    OR email LIKE CONCAT('%', #{keyword}, '%')
                    OR username LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 상점관리자 총 개수 -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*)
        FROM qr_manage_shop_admin
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    name LIKE CONCAT('%', #{keyword}, '%')
                    OR email LIKE CONCAT('%', #{keyword}, '%')
                    OR username LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
    </select>

    <!-- ID로 상점관리자 조회 -->
    <select id="findById" resultMap="shopAdminResultMap">
        SELECT id, username, password, name, email, phone, status, created_at, updated_at
        FROM qr_manage_shop_admin
        WHERE id = #{id}
    </select>

    <!-- 상점관리자 상태 변경 -->
    <update id="updateStatus">
        UPDATE qr_manage_shop_admin
        SET status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 상점관리자 등록 -->
    <insert id="insert" parameterType="co.grap.pack.qrmanage.shopadmin.auth.model.QrManageShopAdmin" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO qr_manage_shop_admin (username, password, name, email, phone, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{status}, NOW(), NOW())
    </insert>

    <!-- 상태별 상점관리자 수 조회 -->
    <select id="countByStatus" resultType="int">
        SELECT COUNT(*)
        FROM qr_manage_shop_admin
        WHERE status = #{status}
    </select>

</mapper>
